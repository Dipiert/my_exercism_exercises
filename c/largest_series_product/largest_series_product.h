#include <stdbool.h>
#include <stddef.h>

unsigned long largest_series_product(char*, size_t);
bool is_span_longer_than_str_length(char*, size_t);
bool is_no_digits(size_t);
bool are_valid_chars(char*);

